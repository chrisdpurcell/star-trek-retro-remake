# Project Status

## Current snapshot

- v0.1 implementation steps 1–10 are complete; the Qt MVC triad runs through `python -m stmrr`.
- The current verification baseline is 11,212 tests, 100% line-and-branch coverage, Ruff, BasedPyright strict over `src`/`tests`/`scripts`, five import contracts, and a clean dependency audit.
- `docs/design/DESIGN.md` is canonical; the PySide6 tech-stack document is supplementary.
- The approved master specification defines the chronological release contract through v1.0.0.
- The approved Step 11 specification is the next implementation milestone: `MapView` and `GridScene` with an empty isometric grid, zoom, pan, and z-switch behavior.
- Project Standards release 5.11.0 and Catalog 5 govern Agent Handoff 1.4, Project Spec 1.4, Python Tooling 1.8, Markdown Frontmatter 1.5, and Markdown Tooling 1.8; Python Coding 0.6 is the reference-only companion.
- Agent Handoff 1.4 provides one shared repo-local Claude/Codex SessionStart runtime and canonical `docs/` status and task paths.
