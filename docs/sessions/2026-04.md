# Sessions — 2026-04

| Date | Headline |
|---|---|
| 2026-04-26 | Design artifacts committed; V2 handoff system landed; spec at `/mnt/share/` renamed to `claude-handoff-system.md`. Commits: 14f226b, fc4255e. |
| 2026-04-26 | Public-facing repo prep + GitHub config (description, 7 topics, secret scanning, vuln reporting); name normalized to "Chris Purcell". Commits: 52a5f54. |
| 2026-04-26 | v0.1 scaffold landed: `pyproject.toml` + `uv.lock` (Python 3.14, PySide6 6.11, tcod 21.2), `src/stmrr/` package skeleton, smoke test, `.importlinter` (end-to-end verified), `.pre-commit-config.yaml`, CI/CD workflows (ci/release/dependency-review), Dependabot, ADRs 0001–0012 + template, CONTRIBUTING/NOTICE/CHANGELOG updates. `DESIGN.md` + `tech-stack-pyside6.md` moved to `docs/design/`. Lesson: import-linter 2.11+ requires `include_external_packages = True` whenever forbidden modules sit outside the root package — `PySide6` and `shiboken6` both qualify. Commits: 85fbc6e, d19fb5d, e50e923, 3f13fd6, ed7d05e, 721dcdd, 9486f34, a45ab03, 1cf71cd, 96e760f. |
| 2026-04-26 | GitHub-side governance applied via `gh api`: merge methods (squash + rebase, no merge commits, auto-update + auto-delete branches), repo topics (7), Actions perms (read+write + can_approve_pr), and **ruleset `main protection`** (ID `15570954`) with `RepositoryRole` admin bypass. Lesson: classic branch protection treats "PR required" as binary; only rulesets expose `bypass_actors` as first-class. Required-status-check context must be the **job name** (`ruff + mypy + import-linter + pytest`), not per-step labels — they all run inside one job. Commits: 4c12e65, 2cad3d5. |
| 2026-04-26 | Docs reconciliation: DESIGN.md §9.1 import-linter example updated to include `include_external_packages = True` (matches what's already in `.importlinter`); ADR-0013 added documenting the ruleset-with-admin-bypass design; ADR tables synced in DESIGN.md §10.7 and `docs/specs-plans.md`. Commit: d46e264. |
| 2026-04-26 | v0.1 model-layer specs authored under `docs/specs/` and cleared via 4 Codex adversarial-review rounds (11 → 7 → 3 → 2 → 0 findings). Spec A (`v0.1-step-3-grid-position-and-game-object.md`, 1187 words) covers `GridPosition` + `GameObject`; Spec C (`v0.1-model-layer.md`, 2429 words) covers cross-cutting concerns — events, ID lifecycle, state machine, action contracts, import DAG, exception hierarchy. Lesson: keep "(resolves SA-XXX)" and "former Q…" review breadcrumbs out of the spec body — they pile up across rounds and pass-N reviewers flag them as length bloat; track review status in commits and section headers, not inline. Lesson: assert ground truth about library behavior (e.g., Blinker receiver order, mypy strict acceptance) only after checking the docs — review pass 2 caught a fabricated "in registration order" guarantee, pass 4 caught a wrong claim about mypy's int-acceptance. Commits: c87855e, 1187b65, 87c619d, e36a206, 9bb67d3, 86da588, 3f70794, d39282f, 525a441, 680b015. |
| 2026-04-27 | Pruned `docs/state.md` below the V2 eager-load budget while preserving current milestone and pickup pointers. |
| 2026-04-27 | v0.1 step 3 plan authored under `docs/superpowers/plans/2026-04-27-v0.1-step-3-implementation.md`; cleared via three `qdev:quality-review` rounds (5 → 3 → 0 findings). Round 1 caught the E402-from-appended-imports pattern that subagents would have replayed across every task; round 2 caught the `git add -A` recovery regression introduced by round 1's pre-commit recovery fix; round 3 confirmed convergence. Lesson: empirically verify reviewer claims before applying — round 1's CR-001 (pytest --collect-only exits 5) was wrong against this repo's pytest 9.0.3, which exits 0; the corrective patch only added a defensive note. Commits: f9552b0, 50f5d53, e7a8266. |
| 2026-04-27 | v0.1 step 3 implemented via `superpowers:subagent-driven-development`: `GridPosition` (frozen+slotted dataclass, axis validation incl. bool footgun, manhattan/chebyshev/euclidean distance, is_adjacent, 26-neighbor iterator with `(dz,dy,dx)` lex order, `with_*`/`origin`) and `GameObject` (`EntityId` NewType + monotone counter, identity equality, idempotent deactivate, subclass-aware `__repr__` via `type(self).__name__`). 116 tests passing, 100% line+branch on both modules, mypy --strict clean, ruff clean, `import-linter` 2/2 contracts kept (the model-is-Qt-free contract now actively verifies real model code). Lessons: (1) per-task `Lint→Commit` step pairs are required for "every commit green" claims to hold under bisect — relying on a discipline note alone leaves Tasks 4/5/6/8 ungated; (2) `pyproject.toml`'s `[tool.coverage.run] source = ["src/stmrr"]` makes `--cov=<module> --cov-fail-under=100` measure package-wide TOTAL, not the named module — finalize gates must run the full `tests/unit/model/` tree with `--cov` for every non-empty source file (caught at Task 10 execution; corrected in commit 1d657bc); (3) the `# type: ignore[misc]` the plan added on the FrozenInstanceError test was unused on mypy 1.20.2 + Python 3.14, removed in commit c2d5034. Commits: a9c0ab1, 8f32107, f98dcc6, c2d5034, 816f3a8, 05cd103, faa844e, 9230d18, 0ce1849, ac6bffd, a9239fa, 1d657bc. |
| 2026-04-27 | v0.1 step 4 spec authored via `superpowers:brainstorming` and `docs/specs/v0.1-step-4-projection.md` cleared via 3 Codex adversarial-review rounds (7 → 3 → 1 findings + 2 polish items). Locks pure-function module (no PySide6), `(TILE_WIDTH, TILE_HEIGHT) = (64, 32)`, `Z_OFFSET = 32`, `MAX_Z_DEPTH = 10`, diamond/screen iso (x→NE, y→SE), world-origin (negative `sx` permitted), `scene_to_world` returns `Optional[GridPosition]`, round-then-check negativity, `z_value_for(p) = (p.x + p.y) * MAX_Z_DEPTH + p.z` with same-cell ties tolerated. Spec mandates a new `.importlinter` `projection-is-qt-free` contract scoped to `stmrr.view.scene.projection` plus a static AST-import test as redundancy, and four acceptance commands (`lint-imports`, `mypy --strict src/stmrr/view/scene/projection.py`, pytest, pytest with `--cov-reset --cov-fail-under=100`). Pickup: `superpowers:writing-plans` against this spec in next session. Lessons: (1) verify upstream invariants by reading the canonical source before building rationale on them — round 1 caught a load-bearing false claim that "SectorMap forbids same-cell occupancy" (DESIGN.md §4.3 explicitly permits multiple entities per cell; `SectorMap.at` returns `list[GameObject]`); (2) numeric-example self-review must execute every example in every section, not just §4/§5 — round 1 caught a `(-1.0, 16.0)` example in §7 that my pre-commit self-review missed despite executing §4/§5 examples against the formula; (3) when bounding an invariant in §8, immediately grep §7 for matching test obligations and propagate the bound — round 3 caught the §7 painter-correctness row still saying "regardless of z" while §8 invariants had been bounded to `MAX_Z_DEPTH` in round 2; (4) `pyproject.toml`'s `addopts = "--cov=stmrr ..."` accumulates with downstream `--cov` flags — `--cov-reset` is mandatory when targeting a single module with `--cov-fail-under` (round 2 finding, same root cause as step 3's coverage gotcha but the inverse direction). Commits: f6474b5, a985c60, c3a56a8, cbd62bc, 00d6cca. |
| 2026-04-27 | v0.1 step 4 plan authored via `superpowers:writing-plans` against the cleared spec at `00d6cca`. 8 tasks, TDD per public function, ending in a hard 100% line+branch coverage gate. Cleared via 1 `qdev:quality-review` round resolving CR-001/002/003: the y-only-negative `scene_to_world` test was using a both-axes-negative input `(0.0, -32.0, 0)` that produced `rounded_x = -1, rounded_y = -1` — masking a buggy `if rounded_x < 0:` (without the `or rounded_y < 0` clause) since the x-clause short-circuits first; `_build_painter_sample` eagerly built a 2800×2799 = 7.8M-pair Cartesian product at test-collection time just to draw 200 samples; file-structure table said "no other files are touched" while Task 8 explicitly modifies `docs/state.md`. Lesson: bipartite branch coverage of `or`-guarded predicates requires inputs that exercise each clause independently — both-axes-negative test cases produce false-pass under one-clause-deletion mutation. Pickup: execute the cleared plan via `superpowers:subagent-driven-development`. Commits: 28b8527, 6cae2a5, dc2505a. |
| 2026-04-27 | v0.1 step 4 implemented via `superpowers:subagent-driven-development`: `src/stmrr/view/scene/projection.py` with `world_to_scene` / `scene_to_world` / `z_value_for` + 4 constants, `.importlinter` `projection-is-qt-free` (third contract, 3 kept), `tests/unit/view/scene/test_projection.py` with 10,713 tests including the 2800-position round-trip property test (primary correctness gate), seeded 200-pair random painter sample plus all anti-diagonal-adjacent pairs, AST import-purity guard (belt-and-braces redundancy on the import-linter contract), and z-headroom invariant. 100% line+branch coverage on the targeted module via `--cov-reset` per spec §7; `mypy --strict` clean on the file as a one-off targeted check beyond the view-layer `disallow_untyped_defs` default. 8 tasks each cleared two-stage subagent review (spec compliance + code quality) plus a final branch-level reviewer; final-review tidy-up flipped the spec's status from "Draft — pending review" to "Complete — implemented at bcd07b4" and corrected the state.md SHA pointer. Lessons: (1) deliberately-unused imports in a test file scaffold (intended for later TDD tasks to consume) need an explicit consumer for every imported symbol, or ruff F401 fires at the final pre-commit gate — `MAX_Z_DEPTH` was imported in Task 1's locked import block but no Task 3-7 test referenced it directly, requiring a Task-8 fix-forward that added a spec §8.5 z-headroom invariant test (earning the import AND validating a real spec invariant); (2) ruff's `SIM` rule group includes SIM102 which flags nested `elif: if:` patterns — Task 7's plan code triggered it, fixed inline in Task 8 step 0 by combining into `elif ... and ...`; (3) the painter-sample CR-002 fix from plan review-1 (bounded `while`-loop with `seen`-set dedup over `rng.sample(_FULL_DOMAIN, 2)`) reduces test-collection-time allocation from ~7.8M tuples to ~5,600 while preserving deterministic seeded output. Pickup: step 5 spec authoring per `docs/specs/v0.1-model-layer.md` §4. Commits: 37fc548, 5564c48, c8eb6c1, 129d677, cafed47, 8e9d522, a07e7d0, b33eecf, bcd07b4, e960ab9, 7c0db6d. |
