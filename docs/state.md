# State

**Last updated:** 2026-05-26 (session closeout — step 8 implemented on `main`; step 9 controller-bridge up next)

## State at a glance

- v0.1 scaffold on `main` at HEAD `b7cfbcb`. Step 3 (`GridPosition` + `GameObject`) at `1d657bc`; step 4 (`view/scene/projection.py`) at `bcd07b4`; step 5 (`states.py` placeholder + `exceptions.py` + `events.py`) at `5420568`; step 6 (`sector_map.py` + `station.py`) at `8fe5f8b`; step 7 (`starship.py` + `turn_manager.py`) at `9898551`; **step 8 (`game_state_manager.py` + concrete `MainMenuState`/`SectorMapState`) at `9749cde`** (the index-update commit referenced by the spec/plan rows in `docs/specs-plans.md`; subsequent closeout commits `f2bc214` filled the hash placeholders and `b7cfbcb` prepended the completed-plan neutralizer).
- **Step 8 IMPLEMENTED.** Six commits on `main` via `superpowers:subagent-driven-development` against `docs/superpowers/plans/2026-05-26-v0.1-step-8-implementation.md` (now neutralized): `75ad631` `feat(model/state): MainMenuState + SectorMapState + mutual-reference patch` (Task 2 — two concrete `GameState` subclasses with `pass` `enter`/`exit` bodies per umbrella §5.8, post-class module-level patch resolving the mutual `allowed_transitions` forward reference, `states.py` `__all__` extended 1→3 entries, 13 new tests in `test_states.py` for a per-file total of 21 functions / 22 collected with parametrize); `2c7bd3e` `feat(model/state): GameStateManager with five-step transition lifecycle` (Task 3 — `game_state_manager.py` with `__init__(initial_state)` firing `enter()` synchronously but NOT emitting `state_changed`, read-only `current_state` property over `_current_state`, `transition_to(target)` running the five-step `validate → exit → mutate → enter → emit` lifecycle with `from_state_cls` captured BEFORE mutation, `GameState` imported under `TYPE_CHECKING` only per Q4 lock; `state/__init__.py` replaced with 4 re-exports; 23 new tests in `test_game_state_manager.py` covering construction lifecycle, payload ordering, lifecycle hook exception policy per §6.5, receiver-exception propagation, package re-export contract; one code-quality review iteration applied via amend — replaced 10 `allowed_transitions: Any` probe-class annotations with `ClassVar[frozenset[type[GameState]]]` and fixed a vacuous `len([s0,s1,s2,s3]) == 4` assertion with the actual payload-pair sequence check); `8b82067` `docs(specs): apply step-8 umbrella amendments to v0.1-model-layer.md` (Task 4 — three discrete amendments per spec §10: §4 GSM row expanded with full five-step lifecycle, §4 states.py row expanded with `__init_subclass__` mechanism + post-class patch idiom + §5.8 `pass`-body rationale, §6 DAG row narrowed to move `state.states` from runtime to TYPE_CHECKING column; three-pattern umbrella sync `rg` 3→0 verified); `9749cde` `docs(specs-plans): index v0.1 step-8 spec/plan + verification gate` (Task 5 step 1 — spec row + plan row Status cells amended with `<hash>` placeholder; full validation gate green: ruff format/check clean, mypy --strict src/stmrr clean across 30 files, lint-imports 4/4 contracts kept, model-layer pytest `--cov-reset --cov=stmrr.model --cov-fail-under=100` green at 453 tests, full pytest green at 11168 cases, umbrella sync `rg` 0 matches post-amendment, no-Qt subprocess guard green, invariant-10 enforcement green); `f2bc214` `docs(specs-plans): fill step-8 HEAD hash in spec/plan rows` (Task 5 step 7 — both `<hash>` placeholders replaced with `9749cde` short SHA via `git rev-parse --short HEAD` capture pattern; classic dance because a commit can't reference its own hash); `b7cfbcb` `docs(plans): neutralize completed v0.1 step-8 plan` (Task 5 step 8 — `> **Status: ✅ Complete — DO NOT EXECUTE.**` blockquote prepended to the plan file per `docs/conventions.md` §8 so future agents see the neutralizer before any imperative instruction). **453 model tests at 100% line+branch coverage on `stmrr.model`**; mypy `--strict` clean across 30 source files; 4 import-linter contracts kept (model-is-qt-free, view-does-not-import-model-events, projection-is-qt-free, blinker-only-in-events), 0 broken; full project suite green at 11168 cases. **One runtime DAG edge removed** (umbrella §6 line 271): `state.states` moved from runtime to TYPE_CHECKING column for `state.game_state_manager` since the Q4 lock rejected the `isinstance(target, GameState)` defensive check that would have required a runtime import. With step 8 landed, the v0.1 **controller-pivot model surface is complete**: `GameStateManager` + the two concrete states close out the pure-Python state-machine layer; remaining v0.1 work is the Qt-side bridge (step 9) and the actual MainMenu / SectorMap widgets.
- **Pickup in new session:** step 9 — `controller/model_bridge.py` per umbrella §3 + DESIGN.md §9.1. This is the **first Qt-touching scaffold step**: the bridge subscribes to blinker `state_changed`/`ship_moved`/`docked`/`turn_advanced` signals and re-emits them as `QObject.Signal`s, satisfying the model-is-qt-free contract by keeping all PySide6 imports on the controller side. No spec or plan yet — needs an authoring round followed by adversarial-review pass(es). Start the next session by opening `docs/specs/v0.1-model-layer.md` §3 + DESIGN.md §9.1 + §10.1 to confirm the bridge contract, then either author the step-9 spec directly or invoke `superpowers:brainstorming` if architectural decisions need surfacing first (e.g., signal-naming conventions for Qt re-emission, whether the bridge owns a single QObject or one per signal, error-handling policy when receivers raise).
- Canonical design: `docs/design/DESIGN.md` (wins over `tech-stack-pyside6.md` on conflicts).

## Session Instructions

Before any work:
1. `git -C ~/projects/star-trek-retro-remake pull`.
2. Read `docs/design/DESIGN.md` and `docs/design/tech-stack-pyside6.md`.
3. Check `docs/conventions.md` before new patterns; numbered conventions before session end.
4. Read relevant `docs/adr/` before contradicting a settled decision; path forward = new superseding ADR.
5. **Do not run `uv run pre-commit install`** — global `core.hooksPath` blocks it. Run `uv run pre-commit run --all-files` directly. (memory: `feedback_pre_commit_install.md`)
6. Direct pushes to `main` work (admin bypass); CI must still go green.

## Active incidents

- 🟢 None.
- 🟡 **Full-suite pytest collection count (11168, up from 11129 pre-step-8)** vs. the model-only subset (453, up from 414 pre-step-8) is still suspiciously large. The +39 step-8 delta is the new `test_states.py` + `test_game_state_manager.py` rows; the suspiciously-large baseline (~10,700 cases) predates step 7 and likely comes from a runaway parametrize cartesian product in `tests/unit/test_smoke.py` or `tests/unit/view/scene/test_projection.py`. Not a regression introduced by step 8 — flagged in step-6 + step-7 closeouts as a benign efficiency concern (~5 s runtime), not a correctness one. Worth diagnosing during the first session that touches view-layer tests; until then it remains 🟡.

## What Remains

- **Step 9:** `controller/model_bridge.py` — first Qt-touching scaffold step. Subscribes to blinker signals; re-emits as QObject.Signal. Closes the MVC seam (convention §2). No spec or plan yet — author + adversarial-review in the next session.
- v0.2 deferrals carried forward (combined step-7 + step-8 lists): local `_send_robust` utility (trigger: second non-bridge subscriber lands on any signal — `state_changed` is the fourth signal, currently only the bridge subscribes); `kind: ClassVar[str] → Final[str]` migration; `MissingEntityError(IllegalActionError)` split from `InactiveEntityError`; multi-cell pathing + Pythagorean-diagonal AP cost (`_debit_ap(cost)` is the extension point); faction/reputation gating on `accepts_dock` (step-6 `_Dockable: Protocol` is the extension point); `NotDockableError` split; TOML save/load (v0.2 persistence spec — class-object payload fields like `StateChangedPayload.from_state` will need a fully-qualified-name string mapping with registry-driven reverse lookup per ADR-0004); `__repr__` override on `GameStateManager` and/or concrete states (step-8 spec §9.2 still-open 1); trace logging in `transition_to` (step-8 spec §9.2 still-open 2); reentrant `transition_to` from inside a `state_changed` receiver (step-8 spec §9.1 explicitly unsupported in v0.1, v0.2 may revisit when the bridge or replay layer needs to chain transitions).
- Font license texts due when fonts are committed; `NOTICE.md` has the grep tag.
