# State

**Last updated:** 2026-05-29 (session closeout — **v0.1 step 10 IMPLEMENTED** via `superpowers:subagent-driven-development`. The last IMPLEMENTED step is now step 10 at HEAD `83679b7` (was step 9 at `716e5c0`). Five commits this session: `4fe66f7` feat(view): MainWindow shell + view re-export + import-linter view-contract amendment; `99cfbfd` test(view): MainWindow MVC-seam consumer + branch/lifecycle tests; `7b3e9c2` test(view): assert About text against dynamic __version__ (code-review fix); `3bd5f99` feat(app): build_main_window() seam + main() entry with QApplication-singleton handling; `83679b7` feat(app): python -m stmrr entry via __main__.py.)

## State at a glance

- v0.1 scaffold on `main`. Prior steps: step 3 (`GridPosition` + `GameObject`) `1d657bc`; step 4 (`view/scene/projection.py`) `bcd07b4`; step 5 (`states.py` stub + `exceptions.py` + `events.py`) `5420568`; step 6 (`sector_map.py` + `station.py`) `8fe5f8b`; step 7 (`starship.py` + `turn_manager.py`) `9898551`; step 8 (`game_state_manager.py` + concrete `MainMenuState`/`SectorMapState`) `9749cde`.
- **Step 9 IMPLEMENTED — the MVC seam is closed (HEAD `716e5c0`).** Executed via `superpowers:subagent-driven-development` against `docs/superpowers/plans/2026-05-28-v0.1-step-9-implementation.md` (now neutralized). Commits: `d77cf0d` `feat(controller): ModelBridge blinker→Qt seam + import-linter contract + core tests` (Task 2 — `src/stmrr/controller/model_bridge.py` = `ModelBridge(QObject)`, the **first Qt-touching module**: four class-level `Signal(object)` attrs named identically to the blinker signals, `__init__(manager, parent=None)` connecting four `_on_*` blinker receivers with `weak=False`, read-only `current_state` property delegating to the injected `GameStateManager`, owner-side `teardown()` disconnecting the four blinker subs; **plain `QObject`, NO ABC** (Shiboken metaclass crash); no try/except — on PySide6 6.11.0 a raising view slot routes to `sys.excepthook` and `emit()`/`send()` return normally; the 5th `.importlinter` contract `controller-events-via-bridge-only` + `tests/integration/conftest.py` [repo's **first** conftest, function-scoped `bridge` fixture] + `tests/integration/controller/test_model_bridge.py` core re-emission tests, all in one commit); `9ad4b09` `test(controller): pin ModelBridge teardown, lifetime, slot-exception invariants` (Task 3 — appended teardown/idempotency/owner-side-destruction, end-to-end real-transition, per-signal `weak=False` GC-survival [CR-NEW-002: isolate one connection, drop all refs + `gc.collect()`, assert weakref survival], and SA-001 slot-exception tests using `pytestqt.exceptions.capture_exceptions` not `pytest.raises`; 23 tests total, 100% line+branch on the module); `6850013` `feat(controller): re-export ModelBridge from package __init__` (Task 4 — `controller/__init__.py` now `from stmrr.controller.model_bridge import ModelBridge` + `__all__ == ["ModelBridge"]`); then closeout `716e5c0` (index row Status → implemented) → `68b2106` (fill HEAD hash) → `0c810b4` (neutralize plan) → this state commit.
- **Verification gate green:** `ruff format --check .` clean (61 files), `ruff check .` clean, `mypy --strict src/stmrr` clean across **31 source files** (+1 = `model_bridge.py`), `lint-imports` **5 kept / 0 broken** (new `controller-events-via-bridge-only`), bridge coverage **100% line+branch** (`--cov-reset --cov=stmrr.controller.model_bridge --cov-fail-under=100`, 33 stmts), no-Qt subprocess guard + invariant-10 guard green (35 cases), **full suite 11191 passed** (11168 baseline + 23 new bridge tests), overall coverage 100%.
- **Two-stage per-task review caught 2 real issues, both fixed via amend:** Task 2 code-quality M-1 — the plan's verbatim test had a multi-line signature `ruff format` collapses; ran `ruff format`, amended. Task 3 code-quality minor — `assert result is not None` on blinker's `send()` return is vacuously true (always a list); changed to `assert isinstance(result, list)` per spec §6.2 "bind return value, assert no raise". Task 4 clean. Spec-compliance reviews caught nothing (verbatim-from-plan code matched by construction) — same pattern as steps 7/8.
- Canonical design: `docs/design/DESIGN.md` (wins over `tech-stack-pyside6.md` on conflicts).
- **Step 10 IMPLEMENTED — the MVC triad is runnable (HEAD `83679b7`).** Executed via `superpowers:subagent-driven-development` against `docs/superpowers/plans/2026-05-29-v0.1-step-10-main-window.md` (now neutralized). The **first `QWidget`-class module** (`src/stmrr/view/main_window.py`): `MainWindow(QMainWindow)` shell (menu/toolbar/dock/turn-bar placeholders, gameplay actions `setEnabled(False)`, fullscreen `QAction`); `bind_bridge(bridge)` MVC-seam consumer reading `bridge.current_state` + tracking `state_changed` opaquely; `closeEvent`→`bridge.teardown()` owner-side wiring (step-9 §5.7 deferred, now built). Runnable entry point: `src/stmrr/app.py` (`build_main_window()` testable seam + `main()` reusing `QApplication.instance()`, guarding a non-GUI `QCoreApplication`); `src/stmrr/__main__.py` (`raise SystemExit(main())` — `python -m stmrr` now works). Also: `src/stmrr/view/__init__.py` re-exports `MainWindow`; `.importlinter` view-contract amended with `allow_indirect_imports = True`. **Verification gate green:** ruff + mypy `--strict` clean across **34 source files**, `lint-imports` **5 kept / 0 broken** (import-linter teeth re-verified: direct `view → model.events` still BROKEN), targeted coverage **100% line+branch** on `stmrr.view.main_window` (130 stmts, 12 branches) and `stmrr.app` (21 stmts); in-process-unreachable defensive paths carry documented `# pragma: no cover`. Manual smoke: `QT_QPA_PLATFORM=offscreen python -m stmrr` loguru line printed, exit 124 under `timeout` (real `QApplication`-creating path confirmed). **Full suite 11,212 passed** (11,191 baseline + 21 new tests: 16 in `tests/integration/view/test_main_window.py` + 5 in `tests/integration/test_app.py`). Five commits: `4fe66f7` → `99cfbfd` → `7b3e9c2` → `3bd5f99` → `83679b7`.

## Session Instructions

Before any work:
1. `git -C ~/projects/star-trek-retro-remake pull`.
2. Read `docs/design/DESIGN.md` and `docs/design/tech-stack-pyside6.md`.
3. Check `docs/conventions.md` before new patterns; numbered conventions before session end.
4. Read relevant `docs/adr/` before contradicting a settled decision; path forward = new superseding ADR.
5. **Do not run `uv run pre-commit install`** — global `core.hooksPath` blocks it. Run `uv run pre-commit run --all-files` directly. (memory: `feedback_pre_commit_install.md`)
6. Direct pushes to `main` work (admin bypass); CI must still go green.
7. **Dev tools are an optional extra, not auto-synced** — run `uv sync --all-extras --frozen` before `uv run pytest/ruff/mypy/lint-imports` (matches CI). `uv run <tool>` alone will not install them.

## Active incidents

- 🟢 None.
- 🟢 **Collection-count incident CLOSED (diagnosed benign during step 10).** Root cause: `tests/unit/view/scene/test_projection.py` contributes **~10,700 of 11,212** cases via three parametrized property tests — (1) `test_z_value_for_orders_lower_anti_diagonal_behind_higher` (5,417 cases — painter-ordering over `_STRICT_LT_PAIRS`, pairs where `a.x+a.y < b.x+b.y`); (2) `test_round_trip_scene_to_world_of_world_to_scene_equals_original` (2,800 cases — round-trip invertibility over `_FULL_DOMAIN` = 20×20×7 = 2,800 grid cells, every cell in the sector); (3) `test_z_value_for_orders_lower_z_behind_higher_at_same_anti_diagonal` (2,402 cases — `_SAME_DIAG_LT_Z_PAIRS`: same anti-diagonal, `a.z < b.z`); 5,417+2,800+2,402 = 10,619 dominant cases; remaining ~80 are small parametrizations + single tests. **Root cause:** the full 20×20×7 = 2,800-cell sector domain (`_FULL_DOMAIN`) drives a round-trip sweep over every cell (2,800 cases) and painter-ordering sweeps over adjacent + sampled cell pairs (`_build_painter_sample()` **deliberately avoids** the full 2,800×2,799 ≈ 7.8M-pair cartesian by sampling 200 random pairs plus all in-bounds adjacent pairs). Intentional exhaustive property coverage, pure-math, no I/O, fast. **Verdict: BENIGN — no defect; incident closed.**

## What Remains

- **Step 11 (next):** `src/stmrr/view/scene/map_view.py` + `grid_scene.py` — replace the central-widget placeholder with the real `MapView`/`GridScene` (render empty grid, verify zoom/pan/z-switch). No spec or plan yet — author + adversarial-review in the next session before implementation. Subsequent view steps: `view/scene/items/grid_cell_item.py` + `starship_item.py`.
- **Step-9 v0.2 deferrals (carry forward):** typed `emit_*()` wrappers (`.emit` is `*args: Any`, so they'd add static safety — deferred as small-surface YAGNI, correctness test-pinned); `@Slot(object)` on *view* slots crossing a `QueuedConnection` once a model worker thread lands; local `_send_robust` fault isolation (trigger: a second non-bridge subscriber on any signal); parametric `Signal[T]` typing (no stub path as of May 2026; view slots eat `object` + `isinstance`); Python 3.14 free-threading lock on `send()` (blinker `receivers` dict is unlocked — safe under v0.1 single-thread); bridge registry / multi-bridge ownership.
- **Prior v0.2 deferrals (steps 7–8):** `kind: ClassVar[str] → Final[str]`; `MissingEntityError`/`InactiveEntityError` split; multi-cell pathing + Pythagorean-diagonal AP cost (`_debit_ap(cost)` extension point); faction/reputation gating on `accepts_dock` (`_Dockable: Protocol` extension point); `NotDockableError` split; TOML save/load (class-object payload fields like `StateChangedPayload.from_state` need a fully-qualified-name string mapping with registry-driven reverse lookup per ADR-0004); `__repr__` on `GameStateManager`/concrete states; trace logging in `transition_to`; reentrant `transition_to` from inside a `state_changed` receiver (v0.1 explicitly unsupported).
- Font license texts due when fonts are committed; `NOTICE.md` has the grep tag.
