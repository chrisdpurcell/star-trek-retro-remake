# Spec — v0.1 Pure-Python Model Layer

**Status:** Draft — revision 2; pending re-review
**Date:** 2026-04-26
**Target version:** v0.1
**Author:** Chris Purcell
**Reviewer:** Codex (separate LLM, adversarial pass)

---

## 1. Purpose

The model layer is the simulation — turn logic, AP, entities, sector geometry, state, events. It runs headless under `pytest` with no `QApplication` and no Qt. The view renders what the model says is true; the controller is the seam between Qt input/signals and model calls/events. This spec covers the v0.1 model surface and the cross-module decisions a per-module spec can't see (events, IDs, state-machine, save/load readiness, action contracts). Companion module-level spec: [`v0.1-step-3-grid-position-and-game-object.md`](v0.1-step-3-grid-position-and-game-object.md).

## 2. Scope

**In-scope for v0.1.** The modules required to clear `DESIGN.md` §10.1's v0.1 Definition of Done — the empty grid, the player ship, the End Turn pipeline, and the starbase Dock action.

| DoD item | Requires (model layer) |
|---|---|
| §10.1 #5 click-to-move debits AP, emits `ShipMoved` | `world/grid_position`, `entities/game_object`, `entities/starship` (minimal), `combat/turn_manager`, `events.py` |
| §10.1 #6 Dock action debits AP, emits `Docked` | `entities/station`, `world/sector_map`, `events.py` |
| §10.1 #7 End Turn restores AP, emits `TurnAdvanced` | `combat/turn_manager`, `events.py` |
| §10.1 #4 z-level switching | `world/grid_position` only — UI-side, but coords must be 3-D |
| §10.1 main-menu shell + sector load | `state/game_state_manager`, `state/states` (`MainMenuState`, `SectorMapState`) |

**Out-of-scope for v0.1** (deferred to v0.2+ per `DESIGN.md` §10.1):

- `combat/combat_resolver.py`, weapon/shield damage math (v0.2).
- `combat/ai_state_machine.py` PATROL/ATTACK/FLEE (v0.2).
- `missions/` beyond a placeholder (v0.2 foundation, v0.3 full).
- `resources/resource_manager.py` energy/supplies/morale beyond a placeholder (v0.3).
- `systems/` (weapons, shields, engines, sensors) populated as components (v0.2 onward).
- Save/load round-trip (v0.2 — but the v0.1 entity surface must be designed so the round-trip can be added without breaking changes).
- Procedural galaxy generation, `world/galaxy_map.py` (v0.4).
- Captain XP, crew leveling (v0.5).

Scope creep is the largest project risk per `DESIGN.md` §10.4. Anything outside the table above does not land in v0.1.

## 3. Layering and the Qt-free rule

ADR-0003 ([`docs/adr/0003-model-qt-free.md`](../adr/0003-model-qt-free.md)): *"`src/stmrr/model/` must not import `PySide6`, `shiboken6`, or any other Qt module."* The rule is enforced mechanically by `import-linter`'s `model-is-qt-free` contract in `.importlinter`, run on every CI push and pre-commit. A secondary contract (`view-does-not-import-model-events`) forbids the view from subscribing to model events directly — the only path is through `controller/model_bridge.py`. `DESIGN.md` §9.1 calls this the *"single seam"*.

Every model module starts with `from __future__ import annotations`. Public APIs require complete mypy-strict type hints (`DESIGN.md` §10.6). Naming follows §10.6: `PascalCase` classes, `snake_case` functions, `UPPER_SNAKE_CASE` module constants, **`snake_case` past-tense events** (`ship_moved`, `turn_advanced`) so the bridge translation is mechanical.

## 4. Module-by-module surface (v0.1)

| Module | v0.1 surface | Status |
|---|---|---|
| `model/world/grid_position.py` | Full per spec A. | Foundation |
| `model/entities/game_object.py` | Full per spec A. | Foundation |
| `model/entities/starship.py` | `Starship(GameObject)`: `ship_class: str`, `hull: int`, `ap_remaining: int`, `ap_max: int = 5`. Methods: `move_to(target, sector_map)`, `dock_at(station_id, sector_map)`, `restore_ap()`. **AP lives on the entity** — see §5.4 for the AP contract. Full action specs in §5.6. **No `Faction` field, no shield facings, no weapon/shield/sensor systems in v0.1** — these land at v0.2 per `DESIGN.md` §10.1. | v0.1 — minimal |
| `model/entities/station.py` | `Station(GameObject)`: `station_type: Literal["starbase"]` (only "starbase" in v0.1; remaining literal members `"civilian" \| "military" \| "neutral"` reserved for v0.2+), `services: frozenset[str]` (immutable so equality/hash work cleanly under TOML round-trip). Method: `accepts_dock(ship: Starship) -> bool`. v0.1 always returns `True` for any active `Starship`; v0.2 adds faction/reputation gating. | v0.1 — starbase only |
| `model/world/sector_map.py` | `SectorMap(width: int, height: int, depth: int)`. Internal `_entities: dict[EntityId, GameObject]`. Methods: `add(entity) -> None` (raises `ValueError` if `entity.position` fails `bounds_check`), `remove(entity_id) -> None`, `get(entity_id) -> GameObject \| None`, `at(position) -> list[GameObject]` (returns empty list, never None), `bounds_check(position) -> bool` (returns bool — see §5.7), `entities -> Iterable[GameObject]` (read-only view for `TurnManager` to iterate). All cross-module references resolve through `get`. | v0.1 |
| `model/state/game_state_manager.py` | `GameStateManager` with `current_state: GameState`, `transition_to(target: GameState) -> None`. Validates against `current_state.allowed_transitions`. Emits `state_changed` directly via `model.events`. | v0.1 — MainMenu↔SectorMap |
| `model/state/states.py` | `GameState` ABC (`enter()`, `exit()`, `allowed_transitions: frozenset[type[GameState]]`). v0.1 concrete: `MainMenuState`, `SectorMapState`. | v0.1 — 2 states |
| `model/events.py` | The blinker `Namespace` plus typed `signal()` declarations: `ship_moved`, `docked`, `turn_advanced`, `state_changed`. Payload shape per §5.2. | v0.1 |
| `model/combat/turn_manager.py` | `TurnManager(player_id: EntityId)`. Attrs: `current_turn: int = 1`, `player_id`. Method: `advance_turn(sector_map: SectorMap) -> None` — resolves `sector_map.get(self.player_id)`, asserts result is a `Starship` (concrete class imported at runtime — see §6), calls `player.restore_ap()`, increments turn counter, emits `turn_advanced`. **Does not own AP** (entities do). No initiative, no NPC turn order in v0.1 (`DESIGN.md` §5.2 combat is v0.2). | v0.1 — placeholder for v0.2 |
| `model/exceptions.py` | Single-file exception hierarchy. Root: `ModelError(Exception)`. Two peer subclasses: `IllegalActionError` (action precondition failures — emitted by `Starship.move_to`, `Starship.dock_at`, `TurnManager.advance_turn`) and `IllegalTransitionError` (state-machine guard failures — emitted by `GameStateManager.transition_to`). `IllegalActionError` has the concrete subclasses `InsufficientAPError`, `OutOfBoundsError`, `NotAdjacentError`, `InactiveEntityError`, `NotDockableError`. `IllegalTransitionError` is a leaf (no further subclasses in v0.1). All inherit from `ModelError` (and through it from `Exception`, not `ValueError`) so callers can distinguish model-layer failures from generic value errors raised by, e.g., `GridPosition.__post_init__`. Pure-Python, no dependencies. | v0.1 |

Modules outside the table are **not** part of v0.1. Empty `__init__.py` markers are fine where directories must exist for the layout, but no functional code lands beyond the table above.

## 5. Cross-cutting concerns

### 5.1 Entity ID space

- IDs are `EntityId = NewType("EntityId", int)` minted by a module-level monotone generator in `model/entities/game_object.py` (spec A §5.1). **v0.1 locks monotone `int`** (spec A §9.1, this spec §8.1); UUID4 migration, if any, is a v0.2 persistence-spec concern.
- **Cross-module entity references are by ID, not by object reference.** `TurnManager` references the player as `player_id: EntityId`, not `player: Starship`. `SectorMap.get(id)` is the resolution mechanism.
- **Why ID-based references:** save/load (v0.2) must serialize a graph; object references would force the deserializer to fix up arbitrary cycles, while IDs round-trip through TOML as plain integers and re-resolve at load time via `SectorMap.get`. `DESIGN.md` §9.5 + ADR-0004.
- **Cost:** code that wants to act on an entity does an `O(1)` dict lookup. Acceptable; small entity counts (per `DESIGN.md` §9.4).

### 5.2 Event vocabulary

- Names are `snake_case` past-tense, matching Qt's `Signal` convention so `controller/model_bridge.py` can rename mechanically (`DESIGN.md` §10.6).
- **Payload shape:** `blinker.signal(...).send(sender, **kwargs)`. The first positional argument to `send` is the `sender`; remaining data is keyword-only. The `sender` value is part of the event contract — subscribers can filter by sender, and bridge tests assert on it.
- **Delivery is synchronous for v0.1:** each `send()` call invokes every connected receiver before returning. Queued delivery is a v0.2 problem if combat needs it.
- **Receiver invocation order is undefined.** Blinker's `Signal` stores receivers in a `WeakValueDictionary` (or in `set_class` if customized), so the order in which receivers are called is not guaranteed across Python versions or across reconnects. Receivers **must** be order-independent — no receiver may rely on another running first or last. The `controller/model_bridge.py` is the single subscriber per event in v0.1, which makes the constraint trivial to satisfy; v0.2 must continue to honor it.

| Event | `sender` | Kwargs | Emitted by |
|---|---|---|---|
| `ship_moved` | the `Starship` instance that moved | `ship_id: EntityId`, `from_position: GridPosition`, `to_position: GridPosition` | `Starship.move_to` |
| `docked` | the `Starship` instance that docked | `ship_id: EntityId`, `station_id: EntityId` | `Starship.dock_at` |
| `turn_advanced` | the `TurnManager` instance | `turn_number: int` | `TurnManager.advance_turn` |
| `state_changed` | the `GameStateManager` instance | `from_state: type[GameState]`, `to_state: type[GameState]` | `GameStateManager.transition_to` |

```python
# model/events.py
from blinker import Namespace

stmrr_events = Namespace()

ship_moved = stmrr_events.signal("ship_moved")
"""Sender: Starship. Kwargs: ship_id, from_position, to_position."""

docked = stmrr_events.signal("docked")
"""Sender: Starship. Kwargs: ship_id, station_id."""

turn_advanced = stmrr_events.signal("turn_advanced")
"""Sender: TurnManager. Kwargs: turn_number."""

state_changed = stmrr_events.signal("state_changed")
"""Sender: GameStateManager. Kwargs: from_state, to_state."""
```

- **Where events are declared:** `model/events.py` only. No module-local signals. Single grep target.

### 5.3 State machine contract

Per ADR-0005 ([`docs/adr/0005-hand-rolled-state-machine.md`](../adr/0005-hand-rolled-state-machine.md)) — hand-rolled, ~200 lines, no `transitions` library, no `QStateMachine`.

`GameStateManager.transition_to(target_state)` semantics:

1. Validate that `type(target_state)` is in `current_state.allowed_transitions`. If not, raise `IllegalTransitionError` (declared in `model/exceptions.py`, peer to `IllegalActionError` per §4) with both states named.
2. Call `current_state.exit()`.
3. Update `current_state` reference to `target_state`.
4. Call `target_state.enter()`.
5. Emit `state_changed(from_state=…, to_state=…)`.

The order matters: `exit()` before mutation, `enter()` before emission. Subscribers reading `manager.current_state` inside a `state_changed` handler see the new state. Mutating before exit, or emitting before enter, breaks observer semantics in subtle ways under v0.2's combat ↔ sector transitions.

`GameState` ABC: `enter()`, `exit()`, `allowed_transitions: frozenset[type[GameState]]`. Concrete v0.1 states: `MainMenuState` allows transition to `SectorMapState`; `SectorMapState` allows transition to `MainMenuState`. The full state set lands as features arrive.

### 5.4 Time, turns, AP — single source of truth

**AP lives on the entity, not on the `TurnManager`.** `Starship` owns:

```python
ap_remaining: int   # current pool, debited by actions
ap_max: int = 5     # cap, restored on turn advance, modifiable later by difficulty mode
```

`TurnManager` owns the turn counter and the *orchestration* of restoration; it does not own AP and has no `debit_player_ap` method. The collaboration is:

1. **Action side:** `Starship.move_to(...)` and `Starship.dock_at(...)` debit `self.ap_remaining` directly via an internal `_debit_ap(cost)` helper that raises `InsufficientAPError` if the pool is short. Action specs in §5.6.
2. **Turn-advance side:** `TurnManager.advance_turn(sector_map)` resolves `sector_map.get(self.player_id)`, asserts the result is a `Starship` (concrete-class import per §6), calls `player.restore_ap()`, increments `self.current_turn`, then emits `turn_advanced`.
3. **No reentrancy:** restoration completes *before* the event is emitted, per invariant 3.

`TurnManager` is constructed with a single `player_id: EntityId` for v0.1. v0.2 generalizes when NPC ships join the turn rotation; the v0.1 contract is "one ship gets restored", not "every entity gets restored".

Per `DESIGN.md` §3.1, unused AP does not carry over; `restore_ap()` overwrites, it does not add.

### 5.5 Save/load round-trip readiness

Out of scope to *implement* in v0.1, but every v0.1 model entity must be designed so the round-trip can land in v0.2 without breaking changes. Structural constraints (must hold at the v0.1 entity surface):

- **Plain-attribute state.** No closures, lambdas, generators, or live Qt objects on any entity. Every attribute is a primitive (`int`, `str`, `bool`, `float`), a frozen dataclass (`GridPosition`), an `EntityId`, a `frozenset[str]` (for `Station.services`), or another `GameObject` reference *resolvable by ID*.
- **Subclass discriminator.** Every `GameObject` subclass declares a class-level `kind: ClassVar[str]` literal (`"starship"`, `"station"`). The persistence spec writes this as the TOML table tag and reads it back to dispatch the right subclass constructor. v0.1 lands the `kind` field on `Starship` and `Station` even though no save code reads it yet — adding it later forces a save-format migration we can avoid by paying the byte now.
- **Cross-entity references by `EntityId`** (invariant 8). The persistence layer serializes `dict[EntityId, GameObject]` as a TOML array of tables keyed by `kind`+`id`; `EntityId` round-trips as a plain integer because it's a `NewType` over `int`.
- **Set normalization.** `Station.services: frozenset[str]` serializes as a sorted list and deserializes back into a `frozenset`. The frozen variant gives hash stability and TOML compatibility (TOML has no native set, only arrays). Lock `frozenset` now so v0.2 doesn't migrate from `set`.
- **No file I/O in the model** (invariant 7). The model exports state; the persistence layer writes TOML.

ADR-0004 ([`docs/adr/0004-toml-not-pickle.md`](../adr/0004-toml-not-pickle.md)) bans `pickle` and `dill`.

### 5.6 v0.1 action contracts

The v0.1 vertical slice exercises three actions. Each names a single owner method, a precondition list, an ordered side-effect sequence, and the event it emits. `DESIGN.md` §10.1 DoD items #5, #6, #7 are not satisfied until all three pass tests.

**Error types** (declared in `model/exceptions.py` — see §4 module table): all action preconditions raise an `IllegalActionError` subclass (`InsufficientAPError`, `OutOfBoundsError`, `NotAdjacentError`, `InactiveEntityError`, `NotDockableError`). Subclasses let the controller produce precise error messages without inspecting a string `kind`.

#### 5.6.1 Move

- **Owner:** `Starship.move_to(target: GridPosition, sector_map: SectorMap) -> None`
- **Preconditions** (checked in this order; first failure raises):
  1. `self.active` (else `InactiveEntityError`)
  2. `sector_map.bounds_check(target)` is `True` (else `OutOfBoundsError`)
  3. `self.position.is_adjacent(target)` is `True` (else `NotAdjacentError`) — v0.1 is single-cell moves only; multi-cell pathing is v0.2.
  4. `self.ap_remaining >= 1` (else `InsufficientAPError`) — v0.1 cost is fixed at 1 AP per cell. The Pythagorean-diagonal cost rule (`DESIGN.md` §5.1) lands in v0.2 when movement extends beyond single-cell.
- **Effects** (ordered):
  1. Capture `from_position = self.position` (local, before mutation).
  2. `self.ap_remaining -= 1`.
  3. `self.position = target`.
  4. `ship_moved.send(self, ship_id=self.id, from_position=from_position, to_position=target)`.

#### 5.6.2 Dock

- **Owner:** `Starship.dock_at(station_id: EntityId, sector_map: SectorMap) -> None`
- **Preconditions** (in order):
  1. `self.active` (else `InactiveEntityError`).
  2. `target = sector_map.get(station_id)` is not `None` and `isinstance(target, Station)` and `target.active` (else `NotDockableError` — combined check; the controller doesn't care which sub-failure triggered it).
  3. `self.position.is_adjacent(target.position)` (else `NotAdjacentError`).
  4. `target.accepts_dock(self)` is `True` (else `NotDockableError`). v0.1 always returns `True` for any active `Starship`; the predicate is the v0.2 extension point for faction/reputation gating.
  5. `self.ap_remaining >= 1` (else `InsufficientAPError`).
- **Effects** (ordered):
  1. `self.ap_remaining -= 1`.
  2. `docked.send(self, ship_id=self.id, station_id=station_id)`.
- No state on the `Station` mutates. Service application (repair, resupply, mission briefing) is UI-side / mission-system-side in later versions; v0.1 only proves the action pipeline, per ADR-0011.

#### 5.6.3 End Turn

- **Owner:** `TurnManager.advance_turn(sector_map: SectorMap) -> None`
- **Preconditions:** none — End Turn is always available.
- **Effects** (ordered):
  1. `player = sector_map.get(self.player_id)`. If `None` or not a `Starship`, raise `InactiveEntityError` (defensive — should never happen in v0.1, but the persistence layer will eventually re-instantiate on load and a stale `player_id` is the obvious failure mode).
  2. `player.restore_ap()` — sets `player.ap_remaining = player.ap_max`.
  3. `self.current_turn += 1`.
  4. `turn_advanced.send(self, turn_number=self.current_turn)`.

These three contracts are the v0.1 model's entire action surface. Any other "action" the UI offers in v0.1 (selection, camera pan, z-level switch) is view-side only.

### 5.7 `SectorMap.bounds_check` and `add` policy

**`bounds_check(position) -> bool`** returns `True` iff `0 <= position.x < self.width` and the same for `y < height` and `z < depth`. **Never raises** — it's a predicate, used in conditions.

**`add(entity)`** calls `bounds_check(entity.position)` and raises `ValueError` if it returns `False`, so an out-of-bounds entity never enters the map.

Predicate returns bool, mutator raises on contract violation — a stable pattern across the model layer.

## 6. Module imports — runtime DAG

Authoritative per-module import list. Runtime imports are evaluated at module load; `TYPE_CHECKING` imports are evaluated only by mypy and do not contribute to the runtime DAG.

| Module | Runtime imports from `stmrr.model.*` | `TYPE_CHECKING`-only |
|---|---|---|
| `events` | (none — leaf) | (none) |
| `exceptions` | (none — leaf) | (none) |
| `world.grid_position` | (none — leaf) | (none) |
| `entities.game_object` | `world.grid_position` | (none) |
| `entities.station` | `entities.game_object`, `world.grid_position` | `entities.starship` (`accepts_dock` parameter type) |
| `entities.starship` | `entities.game_object`, `world.grid_position`, `events`, `exceptions` | `world.sector_map`, `entities.station` (action-method parameter types) |
| `world.sector_map` | `world.grid_position`, `entities.game_object`, `exceptions` | (none — duck types entities as `GameObject`) |
| `combat.turn_manager` | `entities.starship`, `world.sector_map`, `events`, `exceptions` | (none) |
| `state.states` | (none — ABC plus concrete states use no other model modules) | (none) |
| `state.game_state_manager` | `state.states`, `events`, `exceptions` | (none) |

**No cycles.** Leaves: `events`, `exceptions`, `grid_position`. `game_object` → `grid_position`. `station`/`starship` → `game_object` + leaves. `sector_map` → `game_object` + leaves. `turn_manager` → `starship` + `sector_map` (no back-edge). `state.*` is an isolated subtree.

**`TurnManager` imports `Starship` at runtime** so `advance_turn` can run `isinstance(entity, Starship)` before `restore_ap()`. No cycle — `Starship` does not import `turn_manager`. Invariant 8 (cross-module references by ID) is unaffected: `TurnManager` stores `player_id`, not a `Starship` reference.

**`SectorMap` does not import `Starship` or `Station`.** It stores `GameObject` instances by `EntityId`; subclass dispatch happens in the action methods.

## 7. Invariants for the model layer

1. No module under `stmrr.model.*` imports `PySide6` or `shiboken6`. Mechanically enforced by `import-linter`.
2. Every active entity is reachable from a `SectorMap` (live entities) or the `GameStateManager`'s active state (transitional). No orphan references; no dangling IDs.
3. Every model event fires *after* the state mutation that justifies it, never before. Subscribers reading mutated state inside an event handler see the new value.
4. The blinker bus in `model/events.py` is the only allowed cross-module change-notification channel. Modules do not hold direct references to each other's mutable state for change notification.
5. All randomness in the model is seeded; given the same seed and input sequence, the simulation is deterministic. Required for replay tests, save/load fidelity, and AI determinism (`DESIGN.md` §5.6).
6. Model code does not call `print` or stdlib `logging` directly; it uses `loguru`.
7. Model code performs no file I/O; persistence is the persistence layer's job.
8. Cross-module entity references are by `EntityId`, never by object reference. Local references inside a single method are fine.
9. State transitions follow exit → mutate → enter → emit. No exceptions.
10. `model/events.py` is the single file declaring signals. No module-local signals.

## 8. Open questions for review

### 8.1 Locked decisions

- **Sync vs. queued events:** synchronous for v0.1 (§5.2). Receiver order is undefined; receivers must be order-independent.
- **`EntityId` stability:** monotone `int` for v0.1. UUID migration, if any, is a v0.2 persistence-spec concern (spec A §9.1).
- **`GameStateManager` emission:** GSM emits `state_changed` directly via `model.events` (§4).
- **Entity authoring:** plain Python classes (mutable) for entity subclasses; `GridPosition` stays a frozen dataclass; pydantic schemas land at the persistence boundary, not on entities.
- **`SectorMap.bounds_check`:** returns `bool`, never raises; `SectorMap.add` raises `ValueError` if `entity.position` fails `bounds_check` (§5.7).
- **`TurnManager` ↔ `Starship` coupling:** `combat/turn_manager.py` imports `Starship` at runtime and uses `isinstance` directly (§6). No `_starship_cls` injection, no `Protocol` indirection.

### 8.2 Still open

1. **`GameStateManager` lifecycle: singleton, DI, or module-level instance?** A singleton couples tests; DI is verbose; a module-level `manager = GameStateManager()` is hard to swap in tests. Proposed answer: construct once in `app.py` and pass into the bridge. Codex: confirm or push back.
2. **Replay-log integration: subscriber to model events, or a side-channel that records action invocations?** The first is decoupled but reconstructs state from events (lossy if events ever lack data); the second is precise but couples replay to every action method. v0.2 mission log + v1.0 replay both need this answered.
3. **Typed event payloads — kwargs vs. dataclasses.** Current spec uses `**kwargs` per blinker's API. Optionally we could declare frozen `ShipMovedPayload`, `DockedPayload`, etc. dataclasses and pass them as a single `payload=` kwarg, getting mypy-strict structural typing for free. Cost: extra type per event; benefit: typo-proofing at the bridge boundary.

## 9. Risks and unknowns

- **State-mutation order.** Subscribers reading `manager.current_state` mid-handler may see the old reference if invariant 9 (exit → mutate → enter → emit) slips. Mitigation: invariants 3 and 9; bridge tests assert post-emit state.
- **`EntityId` migration when persistence lands.** v0.1 monotone `int` IDs reset on process restart, so v0.2 must choose between persisting the counter, switching to UUID4, or rebuilding ID maps at load time. The v0.2 persistence spec owns this decision; v0.1 just keeps the door open per §5.5.
- **State-machine growth past ~7 states.** ADR-0005 already defines the off-ramp ("revisit `transitions` if state count exceeds ~15").
- **Accidental Qt dependency.** `import-linter` is in force as of the v0.1 scaffold; the residual risk is a transient test-time import (e.g., conditional inside a function body) that the static contract misses. Mitigation: a dedicated test that imports every model module and asserts `PySide6` is not in `sys.modules`.
- **Object references vs. IDs.** If `SectorMap` ever stores object references and the persistence layer later wants ID-based serialization, every entity needs auditing. Mitigation: invariant 8 + ID-based references from day one.

## 10. Cross-references

- `DESIGN.md` §3.1 (turn structure, AP), §4.3 (sector shape), §4.4 (cell contents), §5.1 (starship, vector movement), §5.2 (combat sequence — v0.2), §5.6 (AI state machine — v0.2), §9.1 (layering, GameObject sketch, single-thread turn loop), §9.4 (performance, threading), §9.5 (TOML round-trip, save/load), §10.1 (v0.1 phased roadmap + DoD), §10.3 (testing), §10.6 (coding standards).
- `tech-stack-pyside6.md` §11 (scaffold step order).
- ADR-0003 ([`docs/adr/0003-model-qt-free.md`](../adr/0003-model-qt-free.md)) — model-is-Qt-free.
- ADR-0004 ([`docs/adr/0004-toml-not-pickle.md`](../adr/0004-toml-not-pickle.md)) — TOML save format, no pickle.
- ADR-0005 ([`docs/adr/0005-hand-rolled-state-machine.md`](../adr/0005-hand-rolled-state-machine.md)) — hand-rolled state machine.
- ADR-0008 ([`docs/adr/0008-combat-on-sector-grid.md`](../adr/0008-combat-on-sector-grid.md)) — combat on the sector grid; relevant because `SectorMap` is the grid that combat will reuse in v0.2.
- Companion: [`v0.1-step-3-grid-position-and-game-object.md`](v0.1-step-3-grid-position-and-game-object.md) — full module-level detail for the foundation pair.
