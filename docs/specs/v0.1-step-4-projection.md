# Spec — v0.1 Step 4: `view/scene/projection.py`

**Status:** Draft — pending review
**Date:** 2026-04-27
**Target session:** v0.1 view-layer scaffold, step 4 of `docs/design/tech-stack-pyside6.md` §11
**Author:** Chris Purcell
**Reviewer:** Codex (separate LLM, adversarial pass)

---

## 1. Purpose

`src/stmrr/view/scene/projection.py` is the single owner of isometric math: forward projection (world `(x, y, z)` → scene `(sx, sy)`), inverse projection (scene `(sx, sy)` + active z → world `GridPosition` or `None`), and painter-ordering (`zValue` formula for `QGraphicsItem`). Despite living under `src/stmrr/view/`, it imports zero PySide6 — pure Python, fully unit-testable headless, 100% line + branch.

It lands first among view-layer modules per `docs/design/tech-stack-pyside6.md` §11 because every subsequent rendering module (`map_view.py`, `grid_scene.py`, `grid_cell_item.py`, `starship_item.py`) depends on it. Build vertically through the stack early; iso math is the bottom of the view stack.

## 2. Scope

**In-scope:**

- `src/stmrr/view/scene/projection.py` — three public functions, four module-scope constants.
- `src/stmrr/view/scene/__init__.py` re-exports.
- `src/stmrr/view/__init__.py` and `src/stmrr/view/scene/` package markers if not already present.
- Unit tests under `tests/unit/view/scene/test_projection.py`.

**Out-of-scope (deliberately deferred):**

- `QTransform` adapter — `view/scene/map_view.py` (step 6) decides between scene-level transform and per-item precompute. Both paths consume the pure-function API defined here; neither is locked in this spec. See §9.1 for the rationale.
- `setSceneRect()` math, zoom/pan, mouse hit-testing, click → controller dispatch — all step 6 (`map_view.py` / `grid_scene.py`).
- Grid line rendering and the §3.3 question of whether grid lines benefit from a scene-level skew transform. Step 6's call.
- Z-level opacity (active 1.0, inactive 0.35 per `tech-stack-pyside6.md` §3.1) — `grid_scene.py`'s job to apply per item; projection only computes the `zValue` for painter ordering.
- Sector-bounds checking. `SectorMap` owns "is `(x, y, z)` inside the active sector"; projection only enforces non-negativity, the same boundary `GridPosition` enforces in step 3.
- Camera/viewport/scroll/zoom transforms — `MapView` (step 6).
- Any Qt code, signal wiring, controller bridging.

## 3. Layering and constraints

- **File:** `src/stmrr/view/scene/projection.py`. Lives under `view/` for organizational reasons (step 6 will import it from inside `view/scene/`); contains zero PySide6 imports.
- **Imports allowed:** stdlib + `stmrr.model.world.GridPosition` only. No PySide6, no `pydantic`, no `blinker`, no `tcod`, no `loguru` in step 4.
- **Convention §2 exception (explicit):** `docs/conventions.md` §2 says "view modules subscribe to Qt signals exposed by the bridge; they never import from `model/` directly." Read in context, that rule is about *event subscription* — preventing view modules from bypassing the `controller/model_bridge.py` seam to listen to `model.events` directly. It is not about importing *value types* across the layer boundary. The mechanical enforcement (`.importlinter` `view-does-not-import-model-events` contract, lines 14–20 of `.importlinter`) confirms the narrow reading: it forbids `stmrr.model.events` only, not `stmrr.model.*`. `GridPosition` is a value type — frozen, hashable, immutable, no signals — and is the natural input/output for iso math. This spec resolves the convention's textual ambiguity by explicitly permitting `from stmrr.model.world import GridPosition` in `projection.py`. A future docs-cleanup session may reword conventions §2 to match this reading; that is out of scope here.
- **Mechanical no-PySide6 enforcement (in scope for this step):** `.importlinter` currently forbids PySide6 only in `stmrr.model`, not in `stmrr.view`. Step 4's plan must add a third contract scoped to `stmrr.view.scene.projection` (or a more specific subtree) that forbids `PySide6` and `shiboken6`, so the no-Qt promise is enforced by CI rather than honor system. See §7 for the corresponding runtime test as belt-and-braces redundancy. Sample contract:
  ```ini
  [importlinter:contract:projection-is-qt-free]
  name = projection.py must not import Qt or PySide6
  type = forbidden
  source_modules =
      stmrr.view.scene.projection
  forbidden_modules =
      PySide6
      shiboken6
  ```
- `from __future__ import annotations` per `DESIGN.md` §10.6. Type hints are required on every public callable per the repo's view-layer mypy override (`pyproject.toml` lines 98–100: `[[tool.mypy.overrides]] module = "stmrr.view.*"; disallow_untyped_defs = true`). Full `strict = true` is **not** the default for `view/` per `DESIGN.md` §10.6 — but because `projection.py` has zero Qt imports and is pure Python, the implementation must additionally pass `uv run mypy --strict src/stmrr/view/scene/projection.py` as a one-off targeted check during step 4's plan execution. This catches issues like `warn_return_any` and `disallow_any_unimported` that the relaxed view-layer override misses, without forcing a config change.
- Module-level docstring mandatory; per-function docstrings on all three public functions (the *why* for `z_value_for` is non-obvious — it encodes painter ordering, not z-depth — so its docstring carries weight). Constants are self-documenting via name and value.

## 4. Constants and formulas

### 4.1 Constants

```python
TILE_WIDTH: int = 64       # iso diamond width in scene pixels
TILE_HEIGHT: int = 32      # iso diamond height in scene pixels (2:1 dimetric)
Z_OFFSET: int = 32         # vertical scene-pixel rise per +1 world z
MAX_Z_DEPTH: int = 10      # painter-ordering headroom; > max GridPosition.z
```

`TILE_WIDTH:TILE_HEIGHT = 2:1` is the standard dimetric (commonly called "isometric" in game dev) ratio. `Z_OFFSET = TILE_HEIGHT` gives one-tile-height vertical rise per z-step — the boring-correct convention used by Diablo, RimWorld, Project Zomboid, and every other tile-iso engine — so a sprite at `(0, 0, 1)` sits visually directly above `(0, 0, 0)`'s diamond top vertex.

`MAX_Z_DEPTH = 10` is locked headroom over the §4.3 sector ceiling of 7. Any future ADR raising the sector z-ceiling past 9 must confront this constant; see invariant §8.5.

### 4.2 Iso layout choice

**Diamond / screen iso, x-axis northeast, y-axis southeast.** `GridPosition(1, 0, 0)` projects northeast of the origin; `(0, 1, 0)` projects southeast. Higher z renders higher on screen (subtracts from `sy` because Qt's scene-y axis points down). This is the standard `world_to_scene` shape used in every tile-iso reference engine; named explicitly here so a future reader knows immediately which iso variant they're looking at and doesn't try to map staggered-iso intuition onto the formula.

### 4.3 Forward formula

```
sx = (x - y) * TILE_WIDTH / 2
sy = (x + y) * TILE_HEIGHT / 2 - z * Z_OFFSET
```

`world_to_scene` returns `(sx, sy)` as `tuple[float, float]`. `sx` can be negative for cells with `y > x` — that is correct and intended (origin sits at world `(0, 0, 0)`, not at the leftmost visible pixel). `MapView`'s `setSceneRect()` in step 6 handles negative scene coordinates natively.

### 4.4 Inverse formula

```
adjusted_sy = sy + z * Z_OFFSET          # undo the z-lift
raw_x = sx / TILE_WIDTH + adjusted_sy / TILE_HEIGHT
raw_y = adjusted_sy / TILE_HEIGHT - sx / TILE_WIDTH
rounded_x = round(raw_x)
rounded_y = round(raw_y)
if rounded_x < 0 or rounded_y < 0:
    return None
return GridPosition(rounded_x, rounded_y, z)
```

**Round-then-check.** Negativity is checked on `rounded_x` and `rounded_y`, never on `raw_x` or `raw_y`. A click at `sx = -1.0, sy = 0.0` is visually 99% inside the origin tile; it must snap to `(0, 0, z)`, not vanish. Only clicks more than half a tile west or north of the origin's iso position at the active z-level actually fall out of bounds.

**Active-layer offset gotcha.** The "origin" of inverse projection is the active z-level's projected position, not scene `(0, 0)`. At `z = 3`, world `(0, 0, 3)` projects to scene `(0, -96)` because `Z_OFFSET * 3 = 96`. A mouse click at scene `(0, 0)` while the active layer is z=3 inverse-projects to `(3, 3, 3)`, not `(0, 0, 3)` — that's mathematically correct (the click is 96 scene-pixels below the projected layer origin, which is 3 tiles down the iso anti-diagonal). Step 6's mouse handler must pass `sx`/`sy` in scene coordinates, not viewport coordinates, and must understand that "click at scene origin" depends on which layer is active.

**Round-to-nearest.** Python's `round()` (banker's rounding for `.5` ties) is acceptable because the iso-cell hit area is symmetric about its center; the rounding-mode choice doesn't bias hit-testing in practice and the formula is documented as "nearest cell to the click point."

## 5. Function contracts

### 5.1 `world_to_scene`

```python
def world_to_scene(pos: GridPosition) -> tuple[float, float]: ...
```

| Aspect | Detail |
|---|---|
| Semantics | Returns `(sx, sy)` per §4.3. |
| Complexity | O(1). |
| Errors | None. `GridPosition` validation already enforced non-negative integer coordinates upstream. |
| Examples | `world_to_scene(GridPosition(0, 0, 0))` → `(0.0, 0.0)` |
| | `world_to_scene(GridPosition(1, 0, 0))` → `(32.0, 16.0)` |
| | `world_to_scene(GridPosition(0, 1, 0))` → `(-32.0, 16.0)` |
| | `world_to_scene(GridPosition(0, 0, 1))` → `(0.0, -32.0)` |
| | `world_to_scene(GridPosition(19, 19, 6))` → `(0.0, 416.0)` (max-sector corner) |

### 5.2 `scene_to_world`

```python
def scene_to_world(sx: float, sy: float, z: int) -> GridPosition | None: ...
```

| Aspect | Detail |
|---|---|
| Semantics | Inverse-projects `(sx, sy)` against the active z-level per §4.4. Round-to-nearest snapping; `None` if the rounded result has any negative axis. |
| Complexity | O(1). |
| Errors | `ValueError` if `z < 0`. Negative-z input is a caller bug at the function boundary, not a "click missed" — same taxonomy as `GridPosition.__post_init__` (`ValueError` on invalid value, `TypeError` on wrong type, see step 3 spec §4.2). `TypeError` if `z` is not `int` (or is `bool`). `sx`/`sy` accept `int` or `float` (the natural shape from `QPointF.x()`/`.y()` and integer literals). **No runtime guard on `sx`/`sy`** — mypy strict at call sites and the view-layer's `disallow_untyped_defs` catch type errors before they reach this function. Behavior on `nan`/`inf`: `round()` raises `ValueError` on `nan` and `OverflowError` on `inf`, propagating without modification — those are caller bugs, not "click missed." `bool` values for `sx`/`sy` are not guarded against (a `True` would silently arithmetic to `1.0`); they cannot arise in practice from the step 6 mouse path and the trade-off favors a simpler signature. |
| Examples | `scene_to_world(0.0, 0.0, 0)` → `GridPosition(0, 0, 0)` |
| | `scene_to_world(32.0, 16.0, 0)` → `GridPosition(1, 0, 0)` |
| | `scene_to_world(40.0, 18.0, 0)` → `GridPosition(1, 0, 0)` (rounds to nearest) |
| | `scene_to_world(-1.0, 0.0, 0)` → `GridPosition(0, 0, 0)` (raw x ≈ -0.016, rounds to 0 — demonstrates post-rounding negativity rule) |
| | `scene_to_world(-64.0, 0.0, 0)` → `None` (rounds to negative x) |
| | `scene_to_world(0.0, -96.0, 3)` → `GridPosition(0, 0, 3)` (z=3 layer's projected origin sits at scene `(0, -96)`, not `(0, 0)` — see §4.4 note) |
| | `scene_to_world(0.0, 0.0, -1)` → raises `ValueError` |

### 5.3 `z_value_for`

```python
def z_value_for(pos: GridPosition) -> int: ...
```

| Aspect | Detail |
|---|---|
| Semantics | Returns `(pos.x + pos.y) * MAX_Z_DEPTH + pos.z`. The integer is intended for `QGraphicsItem.setZValue()`; higher values paint in front. |
| Complexity | O(1). |
| Errors | None. |
| Examples | `z_value_for(GridPosition(0, 0, 0))` → `0` |
| | `z_value_for(GridPosition(1, 0, 0))` → `10` |
| | `z_value_for(GridPosition(0, 1, 0))` → `10` (intentional tie — see §5.3 note below) |
| | `z_value_for(GridPosition(0, 0, 1))` → `1` |
| | `z_value_for(GridPosition(2, 1, 3))` → `33` |

**Note on intentional ties.** Two distinct positions with the same `(x + y, z)` produce identical `zValue` (e.g., `(1, 0, 0)` and `(0, 1, 0)`). This is **permitted by design**. Two cases produce ties:

1. **Anti-diagonal ties:** distinct cells with the same `(x + y, z)` — e.g., `(1, 0, 0)` and `(0, 1, 0)`. These are visually distinct on screen (different `(sx, sy)` per the forward formula) and the tie only matters if both cells host an item at the same time.
2. **Same-cell ties:** two items at identical `(x, y, z)`. Per `DESIGN.md` §4.3, *"Each cell can contain multiple entities"*, and `SectorMap.at(position) -> list[GameObject]` (per `docs/specs/v0.1-model-layer.md` §4) returns a list, not a unique occupant. Same-cell occupancy is the canonical model state for, e.g., a `Starship` standing on an `Anomaly`. `z_value_for` returns the same integer for every entity in such a cell.

Both cases are resolved by Qt's stable insertion-order stacking for items sharing a `zValue` (`QGraphicsItem::setZValue()` documentation: items with equal Z stack in insertion order). The painter-correctness invariant `z_value_for` enforces is **"no occluding pair across distinct cells inverts"** (§8.3, §8.4) — that's what prevents a high-z foreground tile from disappearing behind a low-z background tile. *Same-cell* sub-ordering (which sprite paints on top when two ships occupy `(5, 5, 0)`) is **not** `z_value_for`'s job; it belongs to the item classes in step 7 (`StarshipItem`, `AnomalyItem`, etc.) and may use insertion order, `EntityId`, item-kind precedence, or any deterministic tiebreaker the rendering layer chooses. Trying to encode same-cell sub-ordering in `z_value_for` would couple projection to entity-kind precedence rules — a step 7 concern leaking into step 4.

## 6. Re-export contract

```python
# src/stmrr/view/scene/__init__.py
from stmrr.view.scene.projection import (
    MAX_Z_DEPTH,
    TILE_HEIGHT,
    TILE_WIDTH,
    Z_OFFSET,
    scene_to_world,
    world_to_scene,
    z_value_for,
)

__all__ = [
    "MAX_Z_DEPTH",
    "TILE_HEIGHT",
    "TILE_WIDTH",
    "Z_OFFSET",
    "scene_to_world",
    "world_to_scene",
    "z_value_for",
]
```

External callers use the package import; internal modules may import the leaf to keep dependency edges visible. Step 3 precedent.

## 7. Test obligations

**Coverage target: 100% line + branch.** Pure Python, no I/O, no Qt, no third-party deps. Project-wide `view/` floor is 60% (`tech-stack-pyside6.md` §9), but pure-math view modules clear 100% with no extra effort.

**Test files:**

- `tests/unit/view/__init__.py` (marker, empty)
- `tests/unit/view/scene/__init__.py` (marker, empty)
- `tests/unit/view/scene/test_projection.py`

**Test cases — `world_to_scene`:**

| Case | Form |
|---|---|
| Origin maps to `(0.0, 0.0)` | inline |
| Unit-x maps to `(TILE_WIDTH / 2, TILE_HEIGHT / 2)` | inline |
| Unit-y maps to `(-TILE_WIDTH / 2, TILE_HEIGHT / 2)` (negative sx — diamond layout) | inline |
| Unit-z maps to `(0, -Z_OFFSET)` | inline |
| Max-sector corner `(19, 19, 6)` produces the expected scene coordinates | inline |
| Equal-x-equal-y positions produce `sx == 0` (anti-diagonal axis) | parametrize over `(0,0)…(19,19)` at z=0 |
| Increasing z monotonically decreases `sy` at fixed `(x, y)` | parametrize over z=0..6 at `(5, 5)` |

**Test cases — `scene_to_world`:**

| Case | Form |
|---|---|
| Exact origin returns `GridPosition(0, 0, 0)` | inline |
| Exact iso unit-x position returns `GridPosition(1, 0, 0)` | inline |
| Mid-cell offset rounds to nearest cell | parametrize over `(sx_offset, sy_offset)` within ±half-tile of cell center |
| Just-outside-origin click (`sx = -1.0, sy = 0.0`) snaps to `GridPosition(0, 0, 0)` after rounding (post-rounding negativity rule; raw `x ≈ -0.016, y ≈ 0.016`, both round to 0) | inline |
| Far-negative click (`sx = -64.0, sy = 0.0`) returns `None` | inline |
| Click rounding to negative y returns `None` | inline |
| Negative `z` raises `ValueError` | parametrize over `z = -1, -5, -100` |
| Non-int `z` (`float`, `bool`) raises `TypeError` | parametrize over `(bad_z,)` |
| `sx`/`sy` accept `int` and `float` interchangeably | parametrize |
| Active z passes through unchanged for in-bounds clicks | parametrize over `z = 0..6` |

**Round-trip property test (the key invariant):**

| Case | Form |
|---|---|
| `scene_to_world(*world_to_scene(p), p.z) == p` for every `p` in the 20×20×7 sector (2800 positions) | parametrize over the full Cartesian product |

This single test pins forward + inverse formula sign-agreement. If z-term signs disagree between §4.3 and §4.4, this test fails on every non-zero-z position. If round-to-nearest snapping is implemented incorrectly, this test fails on the dense interior. It is the spec's primary correctness gate.

**Test cases — `z_value_for`:**

| Case | Form |
|---|---|
| Origin returns `0` | inline |
| Strictly increases as `(x + y)` increases at fixed z | parametrize |
| Strictly increases as `z` increases at fixed `(x + y)` | parametrize over z=0..6 at `(3, 4)` |
| Painter-correctness: for every pair `(p1, p2)` where `p1.x + p1.y < p2.x + p2.y`, `z_value_for(p1) < z_value_for(p2)` regardless of z | parametrize over a representative sampling |
| Painter-correctness: for every pair at same `(x + y)` where `p1.z < p2.z`, `z_value_for(p1) < z_value_for(p2)` | parametrize |
| Intentional ties: `z_value_for(GridPosition(1, 0, 0)) == z_value_for(GridPosition(0, 1, 0))` is documented and tested as the expected behavior | inline |

**Static import-purity test (no PySide6 / no Qt):**

| Case | Form |
|---|---|
| `projection.py` source contains zero `import PySide6`, `from PySide6`, `import shiboken6`, or `from shiboken6` statements (verified by AST scan of the module's source file, not by `sys.modules` inspection — `sys.modules` is contaminated by other test files importing PySide6) | inline |
| `projection.py` imports nothing from `stmrr.controller`, `stmrr.view.docks`, `stmrr.view.dialogs`, `stmrr.view.widgets`, or `stmrr.view.theme` (other-view-subpackage imports indicate a layering smell) | inline |

The static AST scan is belt-and-braces redundancy on top of the `.importlinter` `projection-is-qt-free` contract added in §3. The contract is the static gate (runs in CI alongside `lint-imports`); the AST test runs in pytest and fails fast with a clearer error message ("`projection.py` line N imports `PySide6.QtCore`") than import-linter's contract-violation summary.

**Mandatory CI commands for step 4 acceptance:**

```bash
uv run lint-imports                                                                                # passes the new contract
uv run mypy --strict src/stmrr/view/scene/projection.py                                            # one-off strict check beyond view-layer default
uv run pytest tests/unit/view/scene/test_projection.py                                              # all tests pass
uv run pytest tests/unit/view/scene/test_projection.py --cov=stmrr.view.scene.projection \
    --cov-branch --cov-fail-under=100                                                              # 100% line+branch enforced, not aspirational
```

These commands replace the implicit "tests pass" gate with an enforceable acceptance contract. Step 4's plan must run all four; the coverage command's `--cov-fail-under=100` converts the 100% target from documentation into a hard gate.

**Structure:** AAA with whitespace between phases, no phase comments. Test names: `test_<function>_<scenario>_<expected>`. `pytest.mark.parametrize` with `ids=` for readable CI output. The 2800-position round-trip test uses `ids=lambda p: f"{p.x},{p.y},{p.z}"` so failures point at exact coordinates.

## 8. Invariants

Properties the rest of the view layer relies on; reviewer should probe these.

1. `world_to_scene` is total — defined for every `GridPosition` (which already guarantees non-negative integer coordinates). Never raises.
2. `world_to_scene(GridPosition(0, 0, 0)) == (0.0, 0.0)` exactly. Origin sits at scene origin.
3. **Painter ordering by anti-diagonal:** for any `p1, p2` with `p1.x + p1.y < p2.x + p2.y`, `z_value_for(p1) < z_value_for(p2)`. (Cells further "back" in iso paint behind cells further "forward.")
4. **Painter ordering by z at same anti-diagonal:** for any `p1, p2` with `p1.x + p1.y == p2.x + p2.y` and `p1.z < p2.z`, `z_value_for(p1) < z_value_for(p2)`. (Higher altitude paints in front at the same depth.)
5. **z-headroom invariant:** for every `GridPosition` constructible under `DESIGN.md` §4.3's sector ceiling of `z ≤ 6`, `pos.z < MAX_Z_DEPTH`. A future ADR raising the ceiling past z=9 must update `MAX_Z_DEPTH` or the ordering invariants (§8.3, §8.4) silently break.
6. **Round-trip identity:** `scene_to_world(*world_to_scene(p), p.z) == p` for every `GridPosition p`. (This is the core formula-consistency property; §7 makes it a parametrized test over the full 20×20×7 sector.)
7. `scene_to_world` returns `None` only when post-rounding `x` or `y` is negative; never returns a `GridPosition` with negative coordinates (impossible — `GridPosition.__post_init__` would reject it).
8. `scene_to_world` raises `ValueError` exactly when `z < 0`, and `TypeError` exactly when `z` is non-int (matching step 3's `GridPosition` validation taxonomy).
9. The module imports zero PySide6 / Qt symbols. Mechanically enforced by the new `.importlinter` `projection-is-qt-free` contract added in §3, plus the static AST-import test in §7 as redundancy. Only `stdlib` and `stmrr.model.world.GridPosition` appear in imports.
10. **`z_value_for` is a cell-level / layer-level orderer, not a same-cell sub-orderer.** Two distinct items at identical `(x, y, z)` produce identical `zValue`. Same-cell sub-ordering is a step 7 (item-class) concern.

## 9. Open questions for review

### 9.1 Locked decisions

- **Pure-function module, no PySide6 import.** First view-layer module deliberately keeps Qt out. The §3.3 "apply iso transform once at scene level via `QTransform`" recommendation is a step 6 (`map_view.py`) decision — it applies to the grid-line layer where vector skewing is fine, but is wrong as a blanket scene-level transform for sprite items (sprites would skew unless individually counter-transformed). Per-item precompute via `world_to_scene` is the likely step 6 conclusion for the sprite layer; either way, both paths consume this module's pure-function API. No `as_qtransform()` adapter in this spec.
- **`(TILE_WIDTH, TILE_HEIGHT) = (64, 32)`, `Z_OFFSET = 32`.** A 20×20 sector spans ~1216×608 scene-pixels at 1× zoom — fits comfortably in the 1600×1000 minimum window with chrome budget intact. `Z_OFFSET = TILE_HEIGHT` is the standard one-tile-rise convention.
- **`MAX_Z_DEPTH = 10`.** Locked headroom over the §4.3 ceiling of 7. Invariant §8.5 pins it.
- **Diamond / screen iso (x → NE, y → SE).** Formula in §4.3, §4.4 is explicit and documented; not staggered-iso.
- **World origin, negative `sx` permitted.** Decouples projection from sector dimensions. `MapView`'s `setSceneRect()` in step 6 handles negative scene coordinates natively.
- **Constants live in `projection.py` as their permanent location**, not deferred-pending-step-6. Tile pixel dimensions are *geometric* parameters of the iso formula, not theme. Theme (`view/theme/palette.py`) holds colors and fonts; projection holds geometry. See §10 cross-ref note.
- **`scene_to_world` signature: `(sx: float, sy: float, z: int) -> GridPosition | None`.** Single function, returns `None` when post-rounding result has any negative axis. Sector-bounds checking is `SectorMap`'s job.
- **Round-then-check negativity.** Round first, then check `rounded_x < 0 or rounded_y < 0`. Clicks just outside the origin tile snap to `(0, 0)` rather than vanishing.
- **Negative z is a `ValueError` at the function boundary**, not a `None`. Caller bug, not a "click missed."
- **Painter formula `(x + y) * MAX_Z_DEPTH + z`, ties permitted.** The painter-correctness invariant `z_value_for` enforces is **"no occluding pair across distinct cells inverts"** (§8.3, §8.4), not "no ties period." Two tie cases exist: anti-diagonal ties (`(1, 0, 0)` and `(0, 1, 0)`) and same-cell ties (per `DESIGN.md` §4.3, *"Each cell can contain multiple entities"* — `SectorMap.at(position) -> list[GameObject]` per `docs/specs/v0.1-model-layer.md` §4 returns a list, not a unique occupant). Both cases resolve via Qt's stable insertion-order stacking. Same-cell sub-ordering — which sprite paints on top when two ships occupy `(5, 5, 0)` — is a step 7 item-class concern, not `z_value_for`'s job. Encoding entity-kind precedence in the projection formula would couple step 4 to entity-class details.
- **`mypy --strict` is a step-4 acceptance gate, not a repo-wide config change.** The repo's `view/` mypy override (`pyproject.toml` lines 98–100) gives `view.*` only `disallow_untyped_defs = true` per `DESIGN.md` §10.6's per-module strictness scheme. Step 4's plan adds `uv run mypy --strict src/stmrr/view/scene/projection.py` as a one-off targeted command in the CI gate (§7) — the file is pure Python and earns strict treatment without forcing the rest of `view/` into strict mode.
- **`.importlinter` contract scoped to `projection.py` is in scope for this step.** Current `.importlinter` forbids PySide6 only in `stmrr.model`. Step 4's plan adds a third contract (`projection-is-qt-free`, sample in §3) so the no-Qt guarantee is mechanically enforced, not honor-system. The `.importlinter` edit and the projection.py implementation land in the same plan execution.

### 9.2 Still open

(None for this module. Step 6's scene-level-`QTransform` vs per-item-precompute decision is *its* open question, not ours.)

## 10. Cross-references

- `DESIGN.md` §3.1 (turn structure — irrelevant here but the canonical pointer); §4.3 (sector shape ≤ 20×20×7 — locks `MAX_Z_DEPTH` headroom); §9.1 (layering, view layer's right to import model types); §10.6 (coding standards — `from __future__`, mypy strict).
- `tech-stack-pyside6.md` §2.1 (`view/scene/projection.py` location); §3.1 (z-levels rendered as `zValue` + per-level opacity — projection owns the `zValue` formula, scene owns the opacity); §3.3 (the scene-level `QTransform` recommendation; settled in step 6's spec, not here); §11 (scaffold step 4).
- ADR-0003 ([`docs/adr/0003-model-qt-free.md`](../adr/0003-model-qt-free.md)) — Qt-free model rule. Projection lives under `view/` so the rule technically does not apply, but step 4 voluntarily satisfies it because the math is pure.
- Step 3 spec ([`v0.1-step-3-grid-position-and-game-object.md`](v0.1-step-3-grid-position-and-game-object.md)) — `GridPosition`'s validation taxonomy (`TypeError` on wrong type, `ValueError` on invalid value) is the source for `scene_to_world`'s z-validation taxonomy (§5.2).
- Companion: [`v0.1-model-layer.md`](v0.1-model-layer.md) — cross-cutting model concerns; orthogonal to step 4 but referenced for the `GridPosition` import.
- **Cross-ref note for future readers of `view/theme/`:** tile-pixel constants (`TILE_WIDTH`, `TILE_HEIGHT`, `Z_OFFSET`, `MAX_Z_DEPTH`) live in `projection.py` as permanent location. They are geometric parameters of the iso formula, not theme. Do not redefine them in `view/theme/dimensions.py` or similar; import from `projection` instead.
